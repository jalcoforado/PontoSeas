import { FilterAudiencePipe } from './filter-audience.pipe';

describe('FilterAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
