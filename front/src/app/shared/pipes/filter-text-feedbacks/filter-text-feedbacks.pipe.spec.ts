import { FilterTextFeedbacksPipe } from './filter-text-feedbacks.pipe';

describe('FilterTextFeedbacksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTextFeedbacksPipe();
    expect(pipe).toBeTruthy();
  });
});
