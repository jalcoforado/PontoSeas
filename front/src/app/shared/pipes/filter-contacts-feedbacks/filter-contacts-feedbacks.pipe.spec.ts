import { FilterContactsFeedbacksPipe } from './filter-contacts-feedbacks.pipe';

describe('FilterTextFeedbacksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterContactsFeedbacksPipe();
    expect(pipe).toBeTruthy();
  });
});
