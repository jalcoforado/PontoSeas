import { TypesContactPipe } from './types-contact.pipe';

describe('TypesContactPipe', () => {
  it('create an instance', () => {
    const pipe = new TypesContactPipe();
    expect(pipe).toBeTruthy();
  });
});
