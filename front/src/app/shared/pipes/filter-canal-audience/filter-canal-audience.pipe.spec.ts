import { FilterCanalAudiencePipe } from './filter-canal-audience.pipe';

describe('FilterCanalAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCanalAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
