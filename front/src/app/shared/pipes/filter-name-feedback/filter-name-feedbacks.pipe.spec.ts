import { FilterNameFeedbackPipe } from './filter-name-feedbacks.pipe';

describe('FilterTextFeedbacksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
