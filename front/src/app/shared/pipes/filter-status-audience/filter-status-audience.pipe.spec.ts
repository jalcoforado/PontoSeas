import { FilterStatusAudiencePipe } from './filter-status-audience.pipe';

describe('FilterStatusAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStatusAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
