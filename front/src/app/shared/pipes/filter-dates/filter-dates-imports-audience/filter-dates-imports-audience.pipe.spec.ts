import { FilterDatesImportsAudiencePipe } from './filter-dates-imports-audience.pipe';

describe('FilterDatesImportsAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDatesImportsAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
