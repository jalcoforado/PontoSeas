import { FilterDatesSchenduleAudiencePipe } from './filter-dates-schendule-audience.pipe';

describe('FilterDatesSchenduleAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDatesSchenduleAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
