import { TypeChannelsPipe } from './type-channels.pipe';

describe('TypeChannelsPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeChannelsPipe();
    expect(pipe).toBeTruthy();
  });
});
