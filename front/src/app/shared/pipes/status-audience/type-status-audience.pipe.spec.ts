import { TypeStatusAudiencePipe } from './type-status-audience.pipe';

describe('TypeStatusAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeStatusAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
